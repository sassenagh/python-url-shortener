apiVersion: v1
kind: Service
metadata:
  name: url-shortener-service
  namespace: url-shortener-ns

spec:
  type: LoadBalancer

  selector:
    app: url-shortener

  ports:
    - port: 80
      targetPort: 8000
